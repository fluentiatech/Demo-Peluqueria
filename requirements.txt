# --- Core API ---
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1

# --- Base de datos (async) ---
sqlalchemy==2.0.50
asyncpg==0.31.0          # driver PostgreSQL (producción)
aiosqlite==0.22.1        # driver SQLite (desarrollo local, sin Postgres)
alembic==1.18.4          # migraciones

# --- Sesión / cache ---
redis==8.0.0

# --- LLM (NLU + redacción) ---
openai==2.38.0

# --- Utilidades ---
python-dateutil==2.9.0.post0
httpx==0.28.1

# --- Cifrado de PII en reposo (Fernet + AES-SIV) ---
cryptography==48.0.0

# --- Zona horaria (IANA) disponible aunque la imagen no traiga tzdata del SO ---
tzdata==2026.2

# --- Desarrollo / tests / calidad ---
pytest==9.0.3
pytest-asyncio==1.4.0
ruff==0.15.20            # linter + formateador + escaneo de código (bandit)
mypy==2.1.0              # comprobación de tipos
pip-audit==2.10.1        # escaneo de vulnerabilidades de dependencias
